# 更新日志

所有重要的项目变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [3.1.0] - 2026-08-31

### 新增 🎉
- **核心必读排序** - 国内/国际要闻各自按 LLM 重要性评分挑出最重要的 5-10 条，
  以独立卡片置于该市场所有板块最前面（`mustRead`）
- **休市期间要闻回顾** - 节后首个交易日自动汇总休市期间发生的要闻，
  排在「今日策略建议」之前
- **资金流向板块** - 行业资金流（前 5）+ 个股资金流（前 10），来自东方财富接口
- **国际要闻全文翻译** - 重要性 ≥7 的英文长文抓取正文并整篇译为中文，
  页面提供「查看中文全文」按钮，译文缓存 7 天
- **AI 日报数据洞察** - 页面顶部新增「📊 行业数据洞察」，
  汇总 ARR / 融资 / 用户数 / Token / 定价 五类指标并提供导航锚点
- **CI 缓存** - workflow 缓存 `data/market_data` 与 `translations`，
  让趋势分析有历史快照可比、全文翻译不必每天重抓

### 改进 ✨
- **LLM 调用全面分批** - 区域分类/重要性评分按 20 条一批、指标提取按 12 条一批，
  单批失败只影响该批（回退关键词或默认分），不再整体退化
- **模型可配置** - 分类/翻译/摘要不再硬编码 `gpt-4o-mini`，
  改用 `OPENAI_MODEL_TRANSLATE` / `OPENAI_MODEL_ANALYSIS` / `OPENAI_MODEL_CLASSIFY`，
  workflow 传入默认值
- **翻译失败兜底** - 网关部分超时导致「部分条目仍是英文」时，
  剩余条目自动改用免费接口补翻，而不是原样上页
- **全文翻译重试与失败判定** - 单段失败重试一次；未译字符占比 >30% 判定整篇失败，
  避免中英夹杂的译文进入 7 天缓存反复复用
- **跳过硬付费墙域名** - bloomberg / wsj / ft / economist 等抓取必然 403 的站点
  不再占用每天 5 篇的翻译配额
- **聚合类新闻过滤** - 「XX 周报」「早知道」这类本身就是汇总的条目不再进入要闻列表
- **北向资金板块条件隐藏** - 交易所已停止披露盘中净流入，
  `available=false` 时整块隐藏，不再显示「+0.00 亿」

### 修复 🐛
- **未配置 `OPENAI_BASE_URL` 时静默回退官方地址** - 默认模型名只挂在自建网关上，
  回退必然 401，且会把网关 key 发往第三方；现在三个模块均会显式告警
- **`response_format=json_object` 导致数组被丢弃** - 模型把数组包成
  `{"metrics": [...]}` 后 `isinstance(list)` 判定失败，指标/分类结果整批丢失
- **国内要闻混入美国新闻** - 分类模型 503 后静默退化为关键词兜底，现已由分批容错解决
- **要闻摘要残留 `<span class=` 等 HTML 片段**
- **趋势分析每天误报「全部新进榜 + 全部掉出榜」** - 历史快照（OpenRouter 口径）
  与当前榜单（Artificial Analysis 厂商级口径）名称对不上；
  改为同源比较 + 交集为空时跳过 + 按窗口长度截断历史榜
- **`article_translator` 缺少 `import time`** - 重试路径会抛 `NameError`
- **阿里云函数计算 3 处 bug**：
  - `EXPECTED_RUN_TIME` 默认值 `08:00` 按 UTC 解析，与 workflow 的 UTC 00:00 不符，
    延迟恒为负（实测 -11482 秒），延迟告警永不触发 → 默认值改为 `00:00` 并在文档标注 UTC
  - 运行中（`in_progress`）的 run 没有 `conclusion`，与 `"success"` 比较被判定为
    「运行失败」，每天误报 → 增加状态短路
  - `created_at` / `run_started_at` 缺失时函数直接落到末尾返回 `None`，
    阿里云 FC 判定为调用错误且不产生任何告警 → 补齐 warning 分支

### 文档 📝
- 新增 `ALIYUN_FC_SETUP.md`（阿里云函数计算部署）、`GITHUB_SECRETS_CONFIG.md`、
  `RELEASE_GITHUB.md`

## [3.0.1] - 2026-08-31

### 修复 🐛
- 修复 `monitoring.py` 中文标点导致的语法错误
- 修复 `logger.py` 中文标点导致的语法错误
- 修复 `finance_daily_push.py` 的 `datetime` 导入与变量作用域错误（`UnboundLocalError`）

### 测试 ✅
- 企业级测试通过率 95.8%（23/24）

## [3.0.0] - 2026-08-31

### 新增 🎉
- **监控告警系统** - 完整的监控和告警功能
  - 运行状态监控
  - 多级别告警（INFO/WARNING/ERROR/CRITICAL）
  - 支持企业微信/钉钉/飞书/自定义 Webhook 通知
- **GitHub Actions 推送监测** - 新功能
  - 监测 workflow 运行状态
  - 计算推送延迟（计划时间 vs 实际时间）
  - 生成可视化 HTML 报告
  - 延迟超过阈值自动告警
- **并发抓取优化** - 显著提升性能
  - 多线程并发抓取 RSS 源
  - 失败自动重试（指数退避）
  - 连接池管理
- **结构化日志系统**
  - JSON 格式日志输出
  - Trace ID 支持
  - 敏感信息自动脱敏
  - 日志按天轮转
- **配置管理系统**
  - 统一配置管理
  - 环境变量覆盖支持
  - 配置验证

### 改进 ✨
- **完善 .gitignore** - 防止敏感信息泄露
- **添加 LICENSE** - MIT 许可证
- **固定依赖版本** - requirements.txt 版本锁定
- **完善模块文档** - 所有核心模块增加详细文档字符串
- **版本号管理** - 添加 __version__.py
- **完善 README** - 增加安装章节和详细说明

### 企业级改进 🏢
- 监控装饰器 - 无侵入式集成监控到主程序
- 配置验证工具 - 启动前验证所有必需配置
- 企业级测试套件 - 完整的测试覆盖
- 运维手册 - OPERATIONS.md
- Docker 支持 - Dockerfile

### 测试 ✅
- 综合测试通过率：100%
- 企业级改进测试通过率：83.3%
- 性能测试：并发抓取加速 2-3 倍

### 修复 🐛
- 修复 Windows 编码问题
- 修复配置文件读取错误处理
- 优化错误日志输出

## [2.0.0] - 2026-08-30

### 新增
- **交易日历自动更新** - 从官方数据源获取最新交易日历
- **智能时间扩展** - 周末/节假日后自动扩展数据收集时间
- **市场数据采集** - OpenRouter 和 Artificial Analysis 数据
- **新闻指标提取** - 从新闻中提取 ARR/Token/用户数等关键指标
- **微信公众号发布** - 支持自动发布到微信公众号

### 改进
- 优化 LLM 调用性能
- 增强错误处理
- 完善文档

## [1.0.0] - 2026-08-02

### 新增
- AI 日报推送功能
- 财经日报推送功能
- GitHub Actions 自动化
- GitHub Pages 部署
- 企业微信 Webhook 推送
- HTML 仪表盘生成

---

## 版本规范

### [主版本号.次版本号.修订号]

- **主版本号**：不兼容的 API 修改
- **次版本号**：向下兼容的功能性新增
- **修订号**：向下兼容的问题修正

### 类型标记

- 🎉 **新增**：新功能
- ✨ **改进**：现有功能的增强
- 🐛 **修复**：Bug 修复
- 🏢 **企业级**：企业级特性
- ✅ **测试**：测试相关
- 📝 **文档**：文档更新
- ⚡ **性能**：性能优化
- 🔒 **安全**：安全相关
