# Release Notes - v3.1.0

**发布日期**: 2026年8月31日
**版本类型**: 次版本（Minor Release）
**发布类型**: 稳定版

---

## 📋 版本概述

v3.1.0 的主线是**内容质量**：v3.0.x 解决的是「能不能跑起来」，这一版解决的是
「跑出来的东西对不对、全不全」。

上一版实际推送的日报里存在一批用户可见的问题——今日策略缺失、市场总结显示
「LLM 未配置」、国内要闻混进美国新闻、国际要闻摘要仍是英文、没有全文翻译按钮、
没有资金流分析、摘要里漏出 HTML 片段。排查后发现**根因高度集中**在三处：

1. **模型名硬编码 `gpt-4o-mini`** —— 自建网关上不存在该模型，调用直接 503，
   然后静默回退到关键词兜底/默认分，页面上表现为「功能好像没做」。
2. **单次请求塞太多条** —— 30~100 条一次性送模型，网关等不到上游响应返回 504，
   整批结果全丢。
3. **`response_format=json_object` 把数组包成对象** —— `isinstance(result, list)`
   判定失败，解析出来的指标/分类结果被整批丢弃。

本版围绕这三点做了系统性修复，并补齐了设计文档中已定义但未落地的功能。

**关键改进**:
- 🎉 6 项新功能落地（核心必读、假期回顾、资金流向、全文翻译、数据洞察、CI 缓存）
- 🐛 修复 11 项用户反馈问题 + 阿里云函数计算 3 处 bug
- ✨ LLM 调用全面分批 + 单批失败隔离，不再「一处超时、全盘退化」

---

## 🎉 新增功能

### 1. 核心必读排序（国内 / 国际）
按 LLM 重要性评分（0-10）挑出最重要的 5-10 条，以独立卡片置于该市场所有板块最前面。

- 数据字段：`domestic.mustRead` / `international.mustRead`
- 前端：`.mustread` 卡片，带序号徽标、来源与原文链接
- 实测：国内 2 条、国际 5 条（条数随当日高分新闻数量浮动）

### 2. 休市期间要闻回顾
节后首个交易日自动汇总休市期间发生的要闻，排在「今日策略建议」**之前**——
用户开盘前最先要知道的是休市期间发生了什么。

- 数据字段：`strategy.holidaySummary`、`strategy.isPostHoliday`
- 实测：140 字摘要，`isPostHoliday=True`

### 3. 资金流向板块
- 行业资金流 前 5（净流入 / 净流出）
- 个股资金流 前 10（净流入 / 净流出）
- 数据源：东方财富接口，`scrapers/money_flow_scraper.py` 重构为统一的
  `_get_json` / `_clist` / `_shape_flow` 取数管线

> 北向资金已被沪深交易所停止盘中披露，接口只剩占位 0。现在 `available=false`
> 时整块隐藏，不再显示会被误读为真实行情的「+0.00 亿」。

### 4. 国际要闻全文翻译
重要性 ≥7 的英文长文抓取正文并整篇译为中文，页面提供「查看中文全文」按钮。

- 译文缓存 7 天（`translations/`，CI 内由 `actions/cache` 恢复）
- 单段翻译失败重试 1 次；未译字符占比 >30% 判定整篇失败，
  避免中英夹杂的「译文」进缓存后被反复复用
- 跳过硬付费墙域名（bloomberg / wsj / ft / economist / barrons / nytimes /
  seekingalpha）：这些站点抓取必然 403，换 UA 无效，不排除会把每天 5 篇的
  配额全部占掉（实测曾出现 3 个候选全是 bloomberg.com、最终 0 篇成功）

### 5. AI 日报「📊 行业数据洞察」
页面顶部汇总 ARR / 融资 / 用户数 / Token / 定价 五类指标，并提供导航锚点。

- 实测提取到 6 个指标（Railway B 轮 1 亿美元、Lambda 债务融资 10 亿美元、
  Railway 开发者 200 万、Claude Code 月费 200 USD、Uber 会话成本降幅、
  Uber PR 占比）

### 6. CI 缓存
workflow 新增两个 `actions/cache` 步骤：

| 缓存路径 | 作用 |
|---|---|
| `data/market_data` | 趋势分析需要历史快照对比；不缓存则永远报「无足够历史数据」 |
| `translations` | 不缓存则每天把同一批文章重新抓取 + 重新过一遍 LLM |

---

## ✨ 改进

### LLM 调用全面分批 + 单批失败隔离

| 任务 | 批大小 | 单批失败时 |
|---|---|---|
| 区域分类 | 20 条 | 该批回退关键词分类 |
| 重要性评分 | 20 条 | 该批用默认分 5 |
| 指标提取 | 12 条 | 跳过该批 |
| 全文翻译 | 3000 字符 | 重试 1 次，仍失败则用原文并计入失败占比 |

改动前是「一次全送 → 504 → 整体退化」，现在最差情况只损失一批。

### 模型可配置，不再硬编码

```
OPENAI_MODEL_TRANSLATE   翻译 / 分类（默认 deepseek-v4-flash，快且便宜）
OPENAI_MODEL_ANALYSIS    市场总结 / 策略建议（默认 gpt-5.6-sol）
OPENAI_MODEL_CLASSIFY    可选，单独指定分类模型
```

workflow 已传入默认值，未配置 Secret 也能跑。

### 翻译失败兜底
网关部分超时导致「部分条目仍是英文」时（例如 21/31 成功），剩余条目自动改用
免费接口补翻。改动前只要 `done > 0` 就直接返回，剩下的 10 条会原样以英文上页。

### 聚合类新闻过滤
「XX 周报」「早知道」这类来源本身就是汇总的条目不再进入要闻列表
（实测过滤掉 3 条，含中文的「霍尔木兹周报」）。

---

## 🐛 Bug 修复

### 1. 未配置 `OPENAI_BASE_URL` 时静默回退官方地址 🔒
**严重性**: P0（功能失效 + 密钥外泄）

默认模型名只挂在自建网关上，官方 OpenAI 没有这些模型，回退必然 401；
更严重的是**自建网关的 API Key 会被发往第三方**，只换回一句语义模糊的 401。
排查「翻译全批失败」时曾被这个静默回退误导。

现在 `ai_daily_push.py` / `llm_helpers.py` / `finance_daily_push.py`
三个模块均在回退到 `api.openai.com` 时显式告警。

> ⚠️ **升级提醒**：若你的 Key 曾在未设置 `OPENAI_BASE_URL` 的情况下运行过，
> 请轮换该 Key。

### 2. `response_format=json_object` 导致结果整批丢弃
模型把数组包成 `{"metrics": [...]}`，`isinstance(result, list)` 判定失败，
指标 / 分类结果被整批丢掉——页面上 ARR / Token / 定价全是空的。
现在统一由 `_extract_json_array()` 解包（贪婪匹配，避免「数组套数组」时
只截到最内层的 `affected_sectors`）。

### 3. 国内要闻混入美国新闻
分类模型 503 后静默退化为关键词兜底，关键词无法区分「美联储」类新闻的归属。
由「模型可配置 + 分批容错」共同解决；同时对模型返回值做规范化
（`"Domestic"` / `"中国"` 等变体统一映射）。

### 4. 要闻摘要残留 `<span class=` 等 HTML 片段
实测复检：0 处残留。

### 5. 趋势分析每天误报「4 新进榜 / 20 掉出榜」
历史快照 `openrouter_*.json` 存的是 OpenRouter 口径模型名
（`"Tencent: Hy4 preview"`），而 `intelligence_rankings` 来自
Artificial Analysis，是厂商级名字（`"Qwen"`、`"DeepSeek"`）。两侧没有一个
名字能对上，于是天天报出「全部新进榜 + 全部掉出榜」的假变化。

修复：只跟同一数据源比 + 交集为空时跳过并注明 + 按窗口长度截断历史榜
（价格榜是历史 rankings 的截断子集，直接比会把「没进前 N」误报成「掉出榜单」）。
复测结果 0 / 0 / 0。

### 6. `article_translator.py` 缺少 `import time`
重试路径调用 `time.sleep()` 会抛 `NameError`。

### 7-9. 阿里云函数计算 3 处 bug

**7. `EXPECTED_RUN_TIME` 默认值时区错误**
默认 `08:00` 被按 UTC 解析，而 workflow 的 cron 是 UTC 00:00，
`delay_seconds` 恒为负（实测 -11482 秒），**延迟告警永不触发**。
默认值改为 `00:00`，文档明确标注该变量为 UTC；负延迟夹到 0 并打印提示。

**8. 运行中的 run 被误判为「运行失败」**
`in_progress` 的 run 没有 `conclusion`（为 `None`），与 `"success"` 比较必然
不等，导致定时器早于 workflow 结束时**每天误报失败**。
增加状态短路：`queued` / `in_progress` / `waiting` / `requested` / `pending`
直接跳过本次判定。

**9. 缺少时间字段时返回 `None`**
`created_at` / `run_started_at` 缺失时函数直接落到末尾返回 `None`，
阿里云 FC 对 `None` 返回值判定为**调用错误**，而且这条路径不产生任何告警，
等于静默失灵。补齐 warning 分支。

---

## ✅ 验证

- 7 个改动的 Python 文件全部通过 `ast.parse`，`U+FFFD` 计数为 0
- `daily.yml` 通过 `yaml.safe_load`
- 阿里云 handler 逐条执行验证：延迟计算由 `-11482 秒` 变为 `+17318 秒`
  （正确识别为一次迟到的手动触发）；告警链路端到端验证
  （`[('ERROR', 'GitHub Actions 运行失败')]`）
- 验证 `ALIYUN_FC_SETUP.md` 中「三个文件、零依赖」的说法：将 3 个文件复制到
  独立目录、从 `sys.path` 剔除仓库路径后可正常运行
- 财经日报 / AI 日报页面重新生成后逐项复检 11 个用户反馈问题

### 已知限制

| 项 | 说明 |
|---|---|
| 国际要闻摘要偶有英文残留 | 网关 504 时补翻兜底会兜住大部分，但网关完全不可用时仍会漏 |
| 全文翻译篇数不稳定 | 取决于当日高分新闻是否落在可抓取的域名上 |
| 北向资金 | 交易所已停止盘中披露，无数据源可用，板块隐藏而非补数 |

---

## 📦 升级说明

无破坏性变更，直接拉取即可。建议同时：

1. **轮换 `OPENAI_API_KEY`**（见 Bug 修复 #1）
2. 确认 GitHub Secrets 中已配置 `OPENAI_BASE_URL`
3. 可选：配置 `OPENAI_MODEL_TRANSLATE` / `OPENAI_MODEL_ANALYSIS`
   覆盖 workflow 默认值
4. 阿里云函数计算用户：将 `EXPECTED_RUN_TIME` 按 **UTC** 重新填写
   （本项目为 `00:00`），并重新上传 `cloudfunction_handler.py`

---

**完整变更**: [v3.0.1...v3.1.0](https://github.com/yunix-intel/ai-daily-push/compare/v3.0.1...v3.1.0)
