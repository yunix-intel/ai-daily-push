# 开发环境配置

environment: dev
debug: true

logging:
  level: DEBUG

llm:
  provider: openai
  model: gpt-3.5-turbo
  api_key: ${LLM_API_KEY}  # 从环境变量读取

push:
  # 开发环境使用 PushPlus
  pushplus_token: ${PUSHPLUS_TOKEN}
