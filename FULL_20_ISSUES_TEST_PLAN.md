# 18 个原始问题 + 2 个衍生问题
# 六步骤全面测试计划

**编制日期：** 2026-09-04  
**测试目标：** 对 18 个原始问题和 2 个衍生问题逐项复测，形成可审计的 PASS / FAIL / BLOCKED 结论。  
**最终环境：** 当前提交触发最新版 GitHub Actions，允许真实企业微信/飞书发送一次日报，并更新正式 GitHub Pages。

---

## 一、测试原则和最终通过门槛

### 1. 证据分级

每个问题至少需要一项直接证据；核心链路问题需要组合证据：

1. **静态证据**：源码、workflow、依赖和模板检查。
2. **单元证据**：确定性输入下的函数级断言。
3. **契约证据**：fixture 输入到 JSON/HTML 输出的字段和页面行为断言。
4. **本地真实端到端证据**：真实网络和真实 LLM 配置下生成两份日报。
5. **线上证据**：GitHub Actions 日志、企业微信/飞书接收结果和正式 Pages 页面。
6. **浏览器证据**：桌面与移动尺寸下的页面结构、交互和控制台无错误。

外部 RSS/API 暂时不可用时，必须记录为“代码降级行为 PASS、真实数据 BLOCKED”，不能把空结果直接算作业务功能 PASS。

### 2. 最终宣布“18+2 全部解决并测试通过”的硬性条件

以下条件必须全部满足：

- 20 项矩阵全部为 `PASS`，不得存在 `FAIL` 或未解释的 `BLOCKED`。
- 全覆盖测试套件为 `0 failures / 0 errors`。
- AI 日报和财经日报真实端到端运行均在明确超时内完成并生成有效 HTML。
- 两份日报均实际发送到真实企业微信/飞书，接收内容和链接可核验。
- 正式 GitHub Pages 更新成功，入口、财经页、历史/监控页均可访问。
- 浏览器桌面和移动端验证通过，控制台无未解释错误，核心按钮交互有效。
- 最新 `daily.yml` 和 `monitor.yml` 日志中无未解释的 Node.js 弃用警告。
- 每一项结论都有命令、关键输出、日志或页面证据路径。

---

## 二、六步骤执行计划总览

| 步骤 | 名称 | 主要产出 | 进入下一步条件 |
|---|---|---|---|
| 1 | 测试基线和环境冻结 | 基线记录、依赖记录、20 项验收矩阵初稿 | 版本、配置、范围已锁定 |
| 2 | 测试基础设施和旧套件修复 | 可重复 fixture、修正后的全覆盖套件 | 测试套件可收集并运行，无基础设施错误 |
| 3 | 单元、静态和契约测试 | 20 项确定性测试结果 | 所有可离线验证项有明确结果 |
| 4 | 两份日报真实端到端运行 | AI/财经日志、HTML、数据核验记录 | 两份任务均完成，或失败原因已定位并修复后重跑 |
| 5 | 浏览器和线上 workflow 验证 | 页面交互证据、Actions 日志、Pages 和消息接收证据 | 正式链路可访问且线上日志通过 |
| 6 | 结果审计、回归和最终判定 | 最终报告、回归日志、20/20 判定 | 所有硬性门槛满足后才可发布通过结论 |

---

# 步骤 1：测试基线和环境冻结

## 1.1 固定测试对象

记录以下信息并写入测试报告：

```bash
git status --short
git rev-parse HEAD
git log -1 --oneline
python --version
python -m pip freeze
```

检查并隔离本地动态产物，尤其是运行日报后生成的 `ai_daily_dashboard.html`、`finance_dashboard.html`、翻译缓存和临时锁文件。不得删除用户已有改动；应在报告中记录哪些文件属于生成物，必要时用临时副本进行测试。

## 1.2 配置和凭据检查

- 确认 `OPENAI_API_KEY`、LLM Base URL、模型、企业微信/飞书 Webhook 等变量只从环境变量或 GitHub Secrets 读取。
- 输出配置名称和是否存在，不输出密钥原文。
- 确认本地离线测试使用 mock/fixture，真实推送只在步骤 4 和步骤 5 的受控运行执行一次。
- 确认正式 Pages 目标仓库、分支和部署目录，避免把测试产物发布到错误项目。

## 1.3 验收矩阵初稿

建立 `20` 行矩阵，每行固定以下字段：

`编号、问题描述、涉及文件/函数、测试层级、输入、通过标准、实际结果、证据路径、结论、重测记录`。

## 1.4 环境边界

- Python 模块和 HTML 生成：本地执行。
- 外部接口成功/失败：fixture 与真实网络各测一次。
- 企业微信/飞书：仅步骤 5 使用真实凭据发送一次受控日报。
- GitHub Pages：仅步骤 5 由最新版 workflow 更新正式页面。
- 不执行删除仓库、重置分支、强制推送或批量发送操作。

**步骤 1 输出：** `baseline.json` 或报告中的基线章节，以及冻结的 20 项矩阵。

---

# 步骤 2：测试基础设施和旧测试套件修复

## 2.1 修正旧测试与当前 API 的偏差

修复 `test_all_functions.py` 中影响执行的测试问题，不改变生产代码行为：

- `self.cls` 改为测试类保存的 `self.module` 或正确的类属性。
- `alerting`、`github_monitor` 的 mock 路径改为实际模块依赖路径。
- `identify_breaking_news()` 补齐当前函数签名所需的 LLM 回调或使用确定性 mock。
- `generate_translation_page()`、封面构建函数使用当前参数名和返回契约。
- `trading_calendar` 测试改为当前实际公开 API，不假设不存在的 `TradingCalendar` 类。
- 保留对异常处理、空数据和降级路径的断言，不能只删掉失败测试。

同时核对 `test_comprehensive_unit.py` 与生产 API，避免两个套件对同一功能使用相互矛盾的预期。

## 2.2 补充确定性 fixture

新增或整理 fixture，至少覆盖：

- 微博成功、单镜像失败、全部镜像失败。
- Twitter rumor、media、时间窗口外条目、全部镜像失败。
- OpenRouter/Artificial Analysis 成功、超时、空响应、模块不可用。
- 英文标题/摘要、全文翻译成功、全文提取失败、LLM 504 重试后失败。
- 北向、板块、个股资金流向分别成功和分别失败。
- 非交易时段指数返回零值，以及上一交易日收盘价回退。
- 旧版列表格式和新版字典格式的 `push_history.json`。

fixture 不得依赖 `example.com` 等不可控外部页面；需要测试失败时，直接 mock HTTP 响应和异常。

## 2.3 稳定输出和执行方式

- 测试输出使用 UTF-8 或 ASCII `[PASS]` / `[FAIL]`，兼容 Windows 控制台。
- 统一使用 `python -m unittest` 或项目既定测试入口。
- 测试失败必须保留完整 traceback 和测试名称。

**步骤 2 通过标准：** 测试套件能完整收集；旧套件和新增套件达到 `0 failures / 0 errors`。若失败是生产缺陷，进入修复后从步骤 2 重新运行。

---

# 步骤 3：20 项单元、静态和契约测试

每项测试都必须断言行为和结果，不能只断言函数或文件存在。

| 编号 | 验证内容 | 必测断言 |
|---|---|---|
| 1 | 推送次数和时间异常 | workflow 有 concurrency；锁在并发/重复执行时拒绝第二实例；历史记录不会重复计数；延迟记录可产生告警 |
| 2 | AI 收录窗口 | 固定当前时间和 cron 环境变量，断言 `windowStart/windowEnd` 为 cron 锚点前 24 小时，且不是实际启动时间 |
| 3 | AI 正文两端对齐 | 生成 HTML 后断言标题和摘要同时包含 `text-align:justify` 与最后一行对齐规则；浏览器 computed style 复核 |
| 4 | 行业数据洞察 | 成功 fixture 生成指标/趋势卡； API 失败、空数据和模块缺失均生成可见降级状态，不静默消失 |
| 5 | AI 翻译按钮 | 英文条目带原文和翻译入口；中文条目不误显示；翻译失败不破坏原文卡片 |
| 6 | 指数 0.00% | 模拟非交易时段和全零行情，断言使用上一交易日数据或明确 stale 状态；正常行情不被覆盖 |
| 7 | 财经收录窗口 | 固定时间跨越 cron 前后，断言财经 `windowStart/windowEnd` 锚定 cron；旧版历史列表可正常读取 |
| 8 | Node.js 弃用警告 | 静态检查所有直接 action 版本；记录新版 workflow 日志，确认没有由旧 action 引起的 Node 20 warning |
| 9 | Pages 导航 | AI、财经、历史/监控入口存在且 href 正确；workflow 将目标 HTML 复制到 Pages 发布目录 |
| 10 | 资金流向 | 北向、板块、个股独立成功/失败；一路失败不影响另外两路；页面显示明确不可用原因 |
| 11 | 国内/国际 Tab | 两类条目带正确 data 属性；点击全部/国内/国际后仅显示对应条目，计数和内容一致 |
| 12 | 突发事件分类 | 国内/国际输入经过分类后不会串区；LLM 回调异常有确定性降级；国际事件不能因错误分组进入国内突发区 |
| 13 | 盘中信息过滤 | 盘中关键词和交易时间组合按规则过滤；盘后普通新闻不被误删；过滤结果可复现 |
| 14 | 财经翻译按钮 | 翻译后的财经条目保留原文链接和翻译标记/按钮；没有翻译结果时不生成失效链接 |
| 15 | 新闻分类逻辑 | 中国/A股/上证/央行等内容优先归国内，即使来源是 Bloomberg/Reuters；纯国际关键词归国际；来源仅作为后备 |
| 16 | 北京 07:00 触发 | `daily.yml` cron 为 UTC 23:00；换算为北京时间 07:00；AI、财经步骤使用相同时间配置 |
| 17 | 唐史主任微博支持 | 配置类型为 `weibo` 时走微博抓取器；平台 URL 为 `weibo.com/u/...`；新浪博客博主仍走博客 URL |
| 18 | X/Twitter 财经抓取 | rumor/media 分组、时间窗口和失败状态均保留；来源链接正确；失败不会让财经日报崩溃 |
| A | Twitter 分类展示 | rumor 和 media 都进入最终 HTML；rumor 显示“未经证实”提示；无数据或失败显示状态卡 |
| B | AI 全文翻译链路 | `link`、`translated_content`、`original_content` 从翻译器进入 shape，再进入 HTML；展开/收起字段完整 |

## 3.1 额外静态检查

```bash
python -m compileall -q .
python -m unittest discover -v
```

核对：

- `requirements.txt` 包含实际需要的依赖，尤其是 HTML 解析依赖。
- workflow 的 `if: always()` 清理锁步骤确实覆盖失败路径。
- 页面内嵌 JSON 可被标准 JSON 解析器读取。
- 不存在硬编码密钥、测试 Webhook 或错误的正式页面域名。

**步骤 3 输出：** 单元/契约测试日志、每项断言结果和更新后的矩阵。

---

# 步骤 4：两份日报真实端到端运行

## 4.1 AI 日报

使用真实网络和真实 LLM 配置，但先使用 `--no-push` 验证构建，记录开始时间、结束时间和各阶段耗时：

```bash
python ai_daily_push.py --no-push --date 2026-09-04
```

要求：

- 设置明确超时；超时后保留日志并定位卡住阶段，不无限等待。
- 检查新闻采集、批量翻译、全文翻译、市场数据、HTML 生成各阶段。
- 解析 `ai_daily_dashboard.html` 内嵌 JSON，断言窗口、条目数、翻译字段、市场指标/趋势或降级卡片。
- 记录 429、超时、SSL 等外部源失败，但确认单源失败不会使任务整体错误退出。
- 确认生成物路径和缓存命中情况。

## 4.2 财经日报

先执行不推送构建：

```bash
python finance_daily_push.py --no-push --hours 24
```

要求：

- 设置比历史 10 分钟更合理但明确的总超时，并为网络抓取阶段设置单独超时。
- 若超时，必须根据最后一条阶段日志定位：新闻/RSS、行情、资金流向、博主、Twitter、LLM、页面生成中的具体阶段。
- 解析财经 HTML 内嵌 JSON，断言行情、窗口、国内/国际、资金流向、博主平台、Twitter 和翻译字段。
- 逐项记录真实数据成功、结构化降级和外部不可用，不把“脚本未崩溃”当作完整通过。

## 4.3 真实消息发送前检查

`--no-push` 构建通过后，核对：

- 接收对象和 Webhook 是预期的真实企业微信/飞书目标。
- 消息内容包含本次测试标识或可从时间戳唯一识别，避免误认为正常历史日报。
- Pages 上传目录只包含本次验证生成的完整页面。
- 不发送重复消息；真实发送只在步骤 5 的最新版 workflow 运行执行。

**步骤 4 通过标准：** AI 和财经两条真实构建链路均完成，或所有失败项已修复并重跑；两份最终 HTML 均可解析且数据契约完整。

---

# 步骤 5：浏览器和线上 workflow 验证

## 5.1 本地页面浏览器验证

启动静态服务，分别验证 AI 和财经页面。每个页面执行：

- `preview_console_logs`：无未解释 error/warning。
- `preview_snapshot`：标题、导航、卡片、降级状态和按钮文本存在。
- `preview_inspect`：窗口、字体、对齐 CSS、按钮和 Tab 的 computed style。
- `preview_click`：AI “翻译全文/收起翻译”；财经“全部/国内/国际” Tab；导航链接。
- `preview_network`：页面引用的内部资源和链接无 404。
- 桌面尺寸和移动尺寸各检查一次，确认文字、Tab、按钮和卡片不溢出或重叠。

重点场景：

- AI 页面显示行业指标/趋势或明确降级卡。
- AI 英文条目可以展开全文翻译，翻译正文实际可见。
- 财经页面显示资金流向成功数据或各子项独立原因。
- 财经页面 rumor/media 分组正确，传言提示可见。
- 博主卡片区分微博和新浪博客链接。
- 财经国内/国际 Tab 切换后条目集合正确。
- 首页到财经和历史/监控页导航可达。

## 5.2 触发最新版 GitHub Actions

确认相关修复已经进入待测提交后，使用 GitHub CLI 触发一次受控运行：

```bash
gh workflow run daily.yml --ref <待测分支或提交对应分支>
gh workflow run monitor.yml --ref <待测分支或提交对应分支>
```

按照仓库实际 workflow 输入补齐必要参数；不重复触发，不并发触发。

检查：

- `daily.yml` 只产生预期的一次运行，concurrency/lock 行为正常。
- AI、财经任务均成功完成。
- 真实企业微信/飞书各收到预期日报，记录接收时间、标题和页面链接。
- Pages 部署 job 成功，正式 URL 返回最新页面。
- 正式页面中的窗口、翻译、市场数据、资金流向、Twitter、Tab 和导航与本地构建一致。
- `monitor.yml` 使用升级后的 action 版本，日志没有 Node 20 弃用警告。
- Actions 日志没有未解释的失败、重试耗尽、密钥缺失或页面上传遗漏。

## 5.3 线上证据保存

保存以下非敏感证据：

- workflow run ID、结论、开始/结束时间。
- 关键日志片段和警告摘要，不保存 Secret。
- 企业微信/飞书消息截图或消息标题、时间、链接记录。
- 正式 Pages URL 和页面验证时间。
- 浏览器交互截图/快照和控制台日志摘要。

**步骤 5 通过标准：** 最新 workflow 成功，真实消息送达，正式 Pages 更新成功，浏览器核心交互通过。

---

# 步骤 6：结果审计、回归运行和最终判定

## 6.1 生成最终矩阵

对每个问题执行证据审计：

- 是否有直接测试输入？
- 是否有明确预期？
- 是否有实际输出？
- 证据是否来自当前待测提交？
- 是否只证明“代码存在”，而没有证明行为？
- 是否受外部服务不可用影响？
- 失败后是否完成修复和重测？

结论只能使用：

- `PASS`：行为和证据均满足验收标准。
- `FAIL`：可复现的代码或部署缺陷。
- `BLOCKED`：外部服务或权限阻断，代码行为尚未得到足够证据。

## 6.2 受影响范围回归

任何修复后至少重新运行：

1. 直接相关单元和契约测试。
2. 两份日报的 `--no-push` 构建。
3. 页面契约解析和浏览器相关交互。
4. 全套测试套件。
5. 若改动 workflow，再运行最新版 Actions 和 monitor 日志检查。

若修复触及共享分类、时间窗口、翻译或页面模板，必须重新运行所有相关问题，而不能只重跑单个失败测试。

## 6.3 最终报告内容

最终 Markdown 报告至少包括：

- 测试对象 commit、环境和依赖。
- 六步执行时间线。
- 20 项矩阵及每项证据链接。
- 测试命令和关键输出摘要。
- AI/财经真实端到端耗时和外部源失败清单。
- 企业微信/飞书发送结果。
- Actions run ID、Pages URL 和 Node.js 警告结论。
- 浏览器桌面/移动验证结论。
- 未解决风险和 BLOCKED 项。
- 最终判定及判定依据。

## 6.4 发布判定

只有在 20 项均为 `PASS` 且满足本计划第一节全部硬性条件时，报告才可以写：

> **18 个原始问题和 2 个衍生问题全部解决，并已通过全面测试。**

否则必须明确写出：

> **当前不能宣称 18+2 全部通过；已通过 X/20，FAIL Y 项，BLOCKED Z 项。**

并列出下一次重测的具体入口、命令和证据要求。

---

## 三、建议执行顺序

1. 先完成步骤 1，冻结 commit、环境和矩阵。
2. 修正旧套件及 fixture，完成步骤 2。
3. 运行步骤 3，先消除确定性失败。
4. 运行 AI/财经真实构建，处理财经日报超时或定位 BLOCKED，完成步骤 4。
5. 使用当前最终提交执行浏览器验证和一次真实线上 workflow，完成步骤 5。
6. 运行全套回归，审计矩阵并形成最终结论，完成步骤 6。

任何步骤出现 FAIL，都回到对应步骤修复后重测；不得用后续页面检查替代前置测试失败，也不得用历史成功运行替代当前提交证据。
